curl https://assets.nagios.com/downloads/nagiosxi/install.sh | sh    // nagios XI install

/usr/local/nagiosxi/scripts/reset_nagiosadmin_password.php --password=newpassword    // to change the nagiosadmin password
